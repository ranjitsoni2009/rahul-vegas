config.stopbubbling = true
lombok.accessors.prefix += _
lombok.anyConstructor.addConstructorProperties = true #needed to make Jackson deserialization and Lombok work nicely together

lombok.log.fieldName = LOG